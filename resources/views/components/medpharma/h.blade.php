<div class="px-2 lg:px-24 xl:px-60">
    @if (isset($h))
    <h2 class="my-8 text-2xl font-black text-gray-800">{{ $h }}
    </h2>
    @endif

    <p class="text-md font-thin tracking-wider text-gray-900">{{ $sh }}</p>
</div>